import styled from 'styled-components';

export const Container = styled.div`
  position: relative;
  grid-area: board;
  padding: 30px;
  /* border: 1px solid ${({ theme }) => theme.button.action}; */
`;
